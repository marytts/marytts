plugins {
    id 'base'
}

allprojects {
    group 'de.dfki.mary'
    version '6.0-SNAPSHOT'

    repositories {
        jcenter()
        maven {
            url 'https://oss.jfrog.org/repo'
        }
    }
}

apply from: 'applicationLogic.gradle'
