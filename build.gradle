plugins {
    id 'application'
}

allprojects {
    group 'de.dfki.mary'
    version '6.0-SNAPSHOT'

    repositories {
        jcenter()
    }
}

dependencies {
    runtime project(':marytts-runtime')
}

mainClassName = 'marytts.server.Mary'
