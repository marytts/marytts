dependencies {
    runtime project('marytts-lexicon-en')
    testCompile libs.testng
    testCompile libs.xmlunit
}

test {
    useTestNG()
}

generateComponentXmlDescriptor {
    locales = ['en-US', 'en-GB']
}
