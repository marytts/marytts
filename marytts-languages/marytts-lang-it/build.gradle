repositories {
    maven {
        url 'https://oss.jfrog.org/artifactory/libs-snapshot'
    }
}

dependencies {
    runtime group: 'de.dfki.mary', name: 'marytts-lexicon-it', version: '0.1.0-SNAPSHOT'
    testCompile libs.testng
}

test {
    useTestNG()
}
